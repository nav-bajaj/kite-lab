# FastAPI and server
fastapi==0.128.6
uvicorn[standard]==0.39.0

# Database
sqlalchemy==2.0.46
alembic==1.16.5
psycopg2-binary==2.9.11

# Data validation
pydantic==2.12.5
pydantic-settings==2.11.0

# Authentication
python-jose[cryptography]==3.5.0

# Scheduling
apscheduler==3.11.2
pytz==2025.2

# HTTP client
httpx==0.28.1

# Data processing
pandas==2.3.3
numpy==2.0.2

# Zerodha API
kiteconnect==5.0.1
pyotp==2.9.0

# Environment
python-dotenv==1.2.1

# Rate limiting
slowapi==0.1.9

# Server-Sent Events
sse-starlette==3.3.0

# Caching
cachetools==6.2.6

# File upload support (required by FastAPI UploadFile)
python-multipart==0.0.20

# Google Drive (cloud_upload scheduled job — Phase 2.5.4 / 2.5.6).
# Required by scripts/upload_to_gdrive.py for the daily 20:30 IST job
# that mirrors /data backups + price-dir snapshots to Drive.
# Without these the cloud_upload job exits with
# ModuleNotFoundError: No module named 'google'.
google-api-python-client==2.196.0
google-auth-httplib2==0.3.1
google-auth-oauthlib==1.3.1
