{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Signal",
  "description": "A market signal — a meaningful movement, theme, event, or concept worth turning into content",
  "type": "object",
  "required": ["title", "signal_type", "why_interesting", "why_now", "who_cares"],
  "properties": {
    "title": {
      "type": "string",
      "description": "Short descriptive title for the signal"
    },
    "signal_type": {
      "type": "string",
      "enum": [
        "momentum",
        "unusual_move",
        "rotation",
        "retail_trap",
        "historical",
        "portfolio",
        "news",
        "event",
        "definition"
      ],
      "description": "Category of signal"
    },
    "ticker_or_theme": {
      "type": "string",
      "description": "Specific ticker, sector, index, or thematic area"
    },
    "why_interesting": {
      "type": "string",
      "description": "What makes this signal worth covering"
    },
    "why_now": {
      "type": "string",
      "description": "Why this signal is timely — what triggered it or makes it urgent"
    },
    "who_cares": {
      "type": "string",
      "description": "Which audience segment this matters to and why"
    },
    "data_points": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Supporting numbers, percentages, or facts"
    },
    "source": {
      "type": "string",
      "description": "Where the signal was identified (e.g. NSE data, RBI bulletin, market observation)"
    },
    "date": {
      "type": "string",
      "format": "date",
      "description": "Date the signal was identified"
    },
    "strength": {
      "type": "string",
      "enum": ["strong", "moderate", "speculative"],
      "description": "How confident we are in this signal's content potential"
    }
  }
}
