# Static Reference Datasets

This directory stores curated, slow-changing datasets used across the project.

- `nse500_universe.csv`: Authoritative list of NSE 500 securities (Company Name, Industry, Symbol, Series, ISIN Code). Update when the exchange revises the index and bump associated checksums/tests.

Add new reference files here with accompanying documentation and validation tests.
