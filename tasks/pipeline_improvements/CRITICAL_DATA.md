# Critical Data Audit (Phase 2.5.3)

What can we afford to lose, and where is each thing protected?

Audit run on 2026-05-16 against a working `backup_database.py --dry-run`
that reached the Railway Postgres via the public proxy and confirmed
the expected tables and row counts.

---

## Replaceable — re-derivable from sources we don't own

These can be regenerated from the Zerodha API or from running the
backtest engine, so we don't need to back them up specifically.

| Asset | Where | Regenerated by |
|---|---|---|
| Daily NSE 500 prices (2020-present) | `nse500_data/` (gitignored) | `scripts/fetch_nse500_history.py` |
| Hourly NSE 500 prices (last 90d) | `nse500_data_hourly/` (gitignored) | same |
| Index histories | `indices_data/` (gitignored) | `scripts/fetch_indices_history.py` |
| Nifty 100 benchmark | `data/benchmarks/nifty100.csv` | `scripts/compute_benchmark.py` |
| Top-25 momentum signals | `data/momentum/top25_signals.csv` | `scripts/build_momentum_signals.py` (manual) |
| Portfolio backtest outputs | `data/{om25_v3,tl25_v3,l6_v2,combo_defensive}_portfolios/<ts>/` | the four portfolio scripts |
| `experiments/` | various | running backtests with new configs |
| Postgres `equity_curve`, `metrics`, `holdings`, `signals` | Railway | re-running backtests + sync_to_database.py |

---

## Irreplaceable — must be redundantly stored

These either come from live execution (Zerodha order placements) or
are manually curated and would take real work to reconstruct.

| Asset | Location | Backup status | Risk now |
|---|---|---|---|
| Live trade ledger | Postgres `trades` (12,418 rows as of 2026-05-16) | ✅ `backup_database.py` to `~/Documents/stock_data/db_backups/` | LOW once daily |
| FIFO P&L matches | Postgres `trade_matches` (6,450 rows) | ✅ same | LOW |
| Current live positions | Postgres `open_positions` (147 rows) | ✅ same | LOW |
| Manual rebalance history | Postgres `rebalances` (0 rows — see note below) | ✅ same | LOW |
| Job execution logs | Postgres `jobs` (179 rows) | ✅ same | LOW |
| Corporate actions | `data/corporate_actions.json` | ✅ git-tracked | LOW |
| Universe definitions | `data/static/nse500_universe.csv` etc. | ✅ git-tracked | LOW |
| Locked strategy configs | `scripts/om25_v3.py`, `tl25_v3.py`, `combo_defensive.py`, `_momentum_engine.py` | ✅ git-tracked | LOW |
| Tuning evidence | `tasks/oos_retune_2026/`, `tasks/MM-tuning/`, `tasks/walk_forward/` | ✅ git-tracked | LOW |
| **2009-2019 GDF price backfill** | `~/Documents/stock_data/nse500_data_historical/` (35 MB) | ❌ local Mac only | **HIGH** |
| `DATABASE_PUBLIC_URL` (Railway Postgres creds) | only in your head + Railway dashboard | ❌ no password-manager entry yet | **HIGH** (if Railway dashboard access lost) |
| Zerodha API credentials (`.env`) | `kite-api/.env`, gitignored | ❌ local only | **MEDIUM** (regeneratable from Zerodha dashboard) |

---

## Gaps to address

### 1. **`nse500_data_historical/` (35 MB, single-Mac risk)**

This is the 2009-2019 backfill purchased from GDF. **Cannot be re-fetched
from Zerodha**, which doesn't go back that far. If this Mac dies:

- The historical OOS retune evidence (`tasks/oos_retune_2026/RESULTS.md`)
  cites results computed from this data
- Walk-forward studies depend on it
- Any future retune attempt against 2009-2019 IS data is blocked

**Decision needed:** commit via git-lfs, upload as part of Phase 2.5.4
to Google Drive, or formally accept the manual-rebuy-only path.

### 2. **Railway DB connection string**

Right now `DATABASE_PUBLIC_URL` is only accessible from the Railway
dashboard. If you lose that account / it's compromised / the Railway
team revokes access, the backup script can't reach the DB to take the
final pre-disaster snapshot.

**Action:** save `DATABASE_PUBLIC_URL` in a password manager you trust
(1Password, Bitwarden, etc.). Update it whenever Railway rotates the
proxy port.

### 3. **`data/static/nifty_smallcap_universe.csv` is untracked**

On disk but never committed. Either:

- Commit it (`git add data/static/nifty_smallcap_universe.csv`) if
  it's a real universe definition you want kept
- Delete it if it was an experiment scratch file

### 4. **`rebalances` table is empty in production**

The `backup_database.py --dry-run` flagged this. Two interpretations:

- You've never used the dashboard's Admin → Rebalance flow (you place
  orders directly via Zerodha Console). 0 rows is expected and OK.
- You have used it, in which case the writes are going somewhere
  unexpected and that's a real data-loss bug.

**If you HAVE used the Rebalance page on the dashboard**, check that
`kite-api/app/api/rebalance.py` actually writes to the `rebalances`
table on order submit. If not, the dashboard is silently losing that
state.

### 5. **Backup is currently local-only**

Even after Phase 2.5.2 lands, backups live on a single Mac. The whole
Phase 2.5 thesis is that the Mac is a single point of failure.

**Mitigation:** Phase 2.5.4 (Google Drive cloud upload) closes this.

---

## Recovery scenarios — see `RECOVERY.md`

A separate document covers the actual restore procedure for each
failure mode (Railway dies / Mac dies / repo lost). This file is
just the inventory — what's at risk and where it lives.
