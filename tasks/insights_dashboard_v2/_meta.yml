name: insights_dashboard_v2
status: in-progress
opened: 2026-08-13
closed: null
branch: insights_dashboard_v2   # branched from auth_stack_v2 (latest)
related_commits:
  - 9aa22f0    # exploration docs
  - ecc6712    # slice plan (indicator-set slices)
  - 3a5dae5    # Slice 1: mission-control shell + Market v1
  - fdf930b    # full-screen app shell
  - 6401924    # Slice 2: Market set complete
  - 1d318b1    # Slice 2.5: atlas columns, metric explorer
  - 4a42387    # founder review round 1
  - 5c3ed75    # regime restore + tab styling + universe concentration
supersedes: []
depends_on:
  - insight_engine      # v1 engine (market-level)
  - insights_v2         # v2 admin launch + stock analytics
  - sector_mapping      # zerodha_sectors.csv + 15 super-sectors (RRG constituent basis)
  - breadth_atlas       # historical breadth panel data/breadth/breadth_daily.csv
  - auth_stack_v2       # launch rides on the new auth stack
register_rows:
  - R-028    # gitleaks allowlist for .pen fileToken (false positive)
# still expected: intraday-data posture row; public /insights exposure row

goals:
  - "Reposition: insight_engine becomes the PRIMARY launch surface (portfolios stay admin-only pending regulatory approval)"
  - "Dashboard-first /insights: every headline indicator = today's value + historical chart (data + charting augmented)"
  - "Intraday refresh during market hours (data-licensing posture decided first)"
  - "RRG: state-of-the-art Relative Rotation Graph, sectors scoped to Nifty 50/100/250/500, with historical scrubbing"
  - "Curated guided lists: volume surges, coiled fresh momentum, custom RS, trend & consistency"
  - "Demote the general screener; the product guides, it is not an empty playground"

governance:
  - "TDD per tasks/insight_engine/TDD_POLICY.md for all new engine/detector work (RRG math, intraday breadth, list detectors)"
  - "VALIDITY_PROTOCOL.md gates any forward-return claim on new lists"
  - "Compliance lexicon (conditions, not instructions) binds all new UI copy"
  - "security-reviewer subagent on any new API surface / sync-path diffs"
